class PlannerAgent:
    def run(self, goal: str, context: dict) -> dict:
        # The planner produces a plan: which tools to call, in what order
        # Uses tool_use / function calling to dispatch to extractor, file reader, etc.
        pass
